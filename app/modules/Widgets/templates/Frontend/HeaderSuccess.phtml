<div id="xrx-header">
	<div class="xrx-wrapper">
		<p><?= $tm->_d(time()); ?></p>

		<? if ($us->isAuthenticated()): ?>
			<p><?= $tm->_('You are logged in.', '.widgets'); ?>
				<a href="<?= $ro->gen('default', array('module'=>'user', 'action'=>'logout')); ?>">
					<?= $tm->_('Log Out', '.widgets'); ?>
				</a>
			</p>
		<? endif; ?>
	</div>
</div>
